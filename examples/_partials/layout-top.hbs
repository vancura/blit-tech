<!doctype html>

<html lang="en">
    <head>
        <meta charset="UTF-8" />
        <meta name="viewport" content="width=device-width, initial-scale=1.0" />
        <title>{{title}}</title>
        <link rel="stylesheet" href="./styles.css" />
        <script>
            if (window.location.hostname !== 'localhost' && window.location.hostname !== '127.0.0.1') {
                var script = document.createElement('script');

                script.async = true;
                script.src = 'https://plausible.io/js/pa-kCVPXRE-G0ez2sSk1JxIN.js';

                document.head.appendChild(script);

                window.plausible = window.plausible || function() {
                    (plausible.q = plausible.q || []).push(arguments)
                };

                plausible.init = plausible.init || function(i) {
                    plausible.o = i || {};
                };

                plausible.init();
            }
        </script>
    </head>

    <body class="font-mono bg-linear-to-br from-bg-from to-bg-to min-h-screen flex items-center justify-center text-white">
        <div class="text-center p-5">
            <h1 class="text-3xl mb-2.5 drop-shadow-lg">{{h1Title}}</h1>

            <div id="canvas-container" class="bg-black border-4 border-accent rounded-lg shadow-2xl inline-block my-5">
                <canvas id="game-canvas" width="320" height="240"></canvas>
            </div>

            <div class="info bg-surface p-4 rounded-lg max-w-xl mx-auto my-5 text-left">
